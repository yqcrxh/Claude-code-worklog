<!-- Append this section to your GLOBAL CLAUDE.md (e.g. ~/.claude/CLAUDE.md). -->

## WORKLOG 工作日志系统

每个项目目录会自动创建 WORKLOG.md 和 WORKLOG_index.md（SessionStart hook，command 类型）。

**写日志的时机：** 用户要求时，或任务告一段落、会话结束前。

**写法：** 先用命令获取系统时间，同时追加到 WORKLOG.md 和 WORKLOG_index.md 两个文件。
每条标题带 `[任务标签]` + session_id（取法见下）。**一个任务可以拆成多条日志，不必合并成一条**，
但同一任务的多条必须用**同一个 `[任务标签]`**——写之前先看 index 末尾有没有现成标签可沿用：
是同一任务就复用，只有新任务才起新标签。日后翻阅时按 `[任务标签]` 归组，一眼分清哪条属于哪个任务。

**取 session_id：** 对话原始记录存在 `~/.claude/projects/<本项目对应目录>/` 下，每段对话
一个 `<session_id>.jsonl`，文件名即 id；当前对话 = 正在写入的那个（修改时间最新）。
若同一文件夹可能开过多个对话、需要确准，读候选 .jsonl 末尾确认含本对话内容，再用它的文件名。

**WORKLOG_index.md**（目录，每条两行）：
```
### YYYY-MM-DD HH:MM [任务标签] (session: xxx)
完整摘要（一句话描述做了什么，信息量足够定位内容）
```

**WORKLOG.md**（详细记录，照搬 index 的时间和摘要后接细节）：
```
### YYYY-MM-DD HH:MM [任务标签] (session: xxx)
完整摘要（与 index 完全一致）

- 做了哪些任务、进度到哪、做得怎样
- **方法**：用了什么方案（有参考价值时写）
- **坑**：遇到什么问题、怎么解决的
- **决策**：为什么这样选择而不是别的方式
```

顺利无特别之处，WORKLOG.md 只写摘要行即可，不需要强行展开。

**关于压缩（compaction）：**
- 压缩会让上下文变模糊。**用户主动 `/compact` 前，最好先写一条当前进度日志**（此时记忆完整，质量最好）。
- 若发生了压缩、细节变模糊，可读对话原始记录找回：上面那个 `<session_id>.jsonl`，
  按上一条日志的时间戳之后的内容来总结。注意 transcript 默认仅保留 30 天。
- agent 类型 hook 在 VSCode 扩展里不支持（会报错），所以不靠自动 agent 写存档，由我手动写。
